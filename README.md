# Loan_Prediction
Built an end-to-end ML classification project to predict loan approvals (Loan_Status) using applicant demographic and financial features. Cleaned data (dropped ID column, encoded categorical features, handled missing values), ran EDA, and benchmarked multiple models. Best result: Random Forest at 82.5% test accuracy.
